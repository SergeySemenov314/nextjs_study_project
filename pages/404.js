import MainContainer from "../components/MainContainer";

const Error = () => {
    return (
        <MainContainer>
            <div>
                <h1>Страница не найдена</h1>
            </div>
        </MainContainer>
    );
};

export default Error;
